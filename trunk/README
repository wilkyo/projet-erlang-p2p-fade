
Pour compiler un jalon, rendez vous dans son dossier et faites make.
Pour exécuter la partie java, faites make java.
Pour exécuter la partie erlang, vous pouvez faire ./run.


H = crypto:sha(term_to_binary(test)).
dave ! {put, self(), H, test}.
eve ! {get, self(), H}.

